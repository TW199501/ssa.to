interface:
  display_name: "Ssa.to"
  short_description: "Repo-specific patterns and workflows for ssa.to"
  default_prompt: "Use the ssa.to repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true